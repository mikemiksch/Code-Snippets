// Takes a string that features line breaks and creates an array of each individual line.

extension String {
    var lines: [String] {
        var result = [String]()
        enumerateLines { line, _ in result.append(line) }
        return result
    }
}
